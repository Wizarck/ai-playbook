# Pre-commit hooks exported by ai-playbook (as a hook provider).
#
# v0.17.0: the `validate-skills-mirror` hook was REMOVED. The single-source
# materialiser (`scripts/materialise_skills.py`) now performs a fingerprint-
# equality short-circuit that obsoletes the drift-detection pre-commit gate.
# Consumers that still list `validate-skills-mirror` in their
# `.pre-commit-config.yaml` get a Tier 3 advisory entry in
# `specs/zombies-manifest.yaml` (`validate-skills-mirror-precommit-hook`) on
# every cleanup run.
#
# No active hooks are exported here at this time. Slice 4 (filesystem reorg,
# v0.18.0) introduces the canonical L1+L2+L3 paired hooks
# (`validate-pairing`, `check-doc-language`, `check-link-integrity`,
# `check-agents-md-size`); those will be registered here.

[]
