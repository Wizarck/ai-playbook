# Flagged for rule migration — Slice 5.A pickup queue

Generated by Sub-slice 5.B (concepts rewrite). Each entry below is a passage in a `docs/concepts/*.md` doc whose normative intent genuinely belongs as a `docs/rules/<slug>.rule.md` rule. Sub-slice 5.A consumes this list as its input queue.

The "flag, don't move" convention: Sub-slice 5.B leaves the original passage in place (with the keyword softened to lowercase) so Sub-slice 5.A can extract it deterministically without rebase pain. The passages here are *candidates* — 5.A makes the final extraction call.

Format per entry: source path + section anchor + 1-line rationale + suggested target rule filename.

## Rule-candidate passages

### 1. PR body must reference tracker issue

- **Source**: `docs/concepts/release-management.md` §4.4 (~L127)
- **Passage**: "The PR body must reference [the tracker_id] via `Closes #N` (GH) or `PROJ-N:` prefix in the title (Jira)."
- **Why this should be a rule**: load-bearing PR-time invariant; failure breaks automatic ticket transitions. Trivial L3 regex check on PR body / title.
- **Suggested target**: `docs/rules/pr-tracker-reference.rule.md` (paired L3 workflow gate).

### 2. Auto-generated PRs must close prior stream PR

- **Source**: `docs/concepts/release-management.md` §4.4 (~L131)
- **Passage**: "Auto-generated PRs (chore/bump-playbook, chore/bump-skills, dependabot, renovate) must close any prior open PR on the same logical change-stream when a newer one opens."
- **Why this should be a rule**: deterministic L1 check on `propagate_bump.py` and equivalents; orphan PR pileup is a real failure mode.
- **Suggested target**: `docs/rules/auto-pr-stream-closure.rule.md` (paired L1 in the propagate scripts).

### 3. Consumer CI required matrix

- **Source**: `docs/concepts/release-management.md` §4.4 (~L148, L168)
- **Passage**: "A consumer project's `.github/workflows/ci.yml` must run [the minimum matrix]" + "CI pre-commit step must invoke the hooks against the diff between the PR's base ref and HEAD, not against the entire repository."
- **Why this should be a rule**: cross-consumer enforcement contract; consumer drift is a recurring incident.
- **Suggested target**: `docs/rules/consumer-ci-matrix.rule.md` (paired L1 in bootstrap + L3 check).

### 4. AI-reviewer signoff loop integration

- **Source**: `docs/concepts/release-management.md` §4.5 (~L233, L236, L238, L262, L278)
- **Passage**: "The worker AI must integrate [AI-reviewer comments] into the loop" + "must inspect the structured Actionable comments section" + "fix commit message must reference the comment ID" + "must invoke L1 self-review" + "must not skip L1 because the diff is obviously trivial".
- **Why this should be a rule**: directly binds worker-agent behaviour; the entire §4.5 subsection is a binding contract, not reference material.
- **Suggested target**: `docs/rules/ai-reviewer-signoff.rule.md` (paired L1 self-check + L3 PR-body marker check).

### 5. Self-review for Profile B (no AI reviewer)

- **Source**: `docs/concepts/release-management.md` §4.5 (~L258)
- **Passage**: "the worker AI must self-review the diff before requesting Gate F".
- **Why this should be a rule**: companion to (4) — fallback rubric when no AI reviewer is available.
- **Suggested target**: rolled into `docs/rules/ai-reviewer-signoff.rule.md` (Profile B branch).

### 6. AI-reviewer signoff body markers

- **Source**: `docs/concepts/release-management.md` §4.5.3 (~L299)
- **Passage**: "The 'AI-reviewer signoff' subsection in the PR body must contain the following three markers (case-sensitive substring match) for L2 to consider it populated."
- **Why this should be a rule**: byte-level PR-body schema — L2 grep check is the canonical L1/L3 invocation.
- **Suggested target**: rolled into `docs/rules/ai-reviewer-signoff.rule.md`.

### 7. Auto-merge discipline

- **Source**: `docs/concepts/release-management.md` §4.5.6 (~L331)
- **Passage**: "The worker AI must not click [Enable auto-merge] before §4.5 is satisfied. Auto-merge is a CONVENIENCE for clean PRs after Gate F approval, not a bypass for the feedback loop."
- **Why this should be a rule**: explicit "must not" — pure binding clause.
- **Suggested target**: `docs/rules/auto-merge-discipline.rule.md` (paired L1 in `gh` wrapper or PostToolUse on the auto-merge tool call).

### 8. Delegated shipping prompt template

- **Source**: `docs/concepts/release-management.md` §4.5.5 + §4.5.6 (~L335, L345)
- **Passage**: "the delegating prompt must embed this directive verbatim" + "must include the literal §4.5.3 canonical block".
- **Why this should be a rule**: prompt-template contract for `Agent(isolation="worktree")`; deterministic L1 check on the spawn envelope.
- **Suggested target**: `docs/rules/delegated-shipping-prompt.rule.md` (paired L1 on Agent-spawn schema).

### 9. GitHub Project Status field schema

- **Source**: `docs/concepts/release-management.md` §6.2 (~L369, L406)
- **Passage**: "every consumer project's GitHub Project (per issue-tracking.md §1) must have a Status field with exactly these five options in this order" + "must carry two text fields populated by the worker AI when a slice transitions Todo → In Progress".
- **Why this should be a rule**: per-consumer board-schema contract; drift breaks `verify_board_state.py`.
- **Suggested target**: `docs/rules/github-project-board-schema.rule.md` (paired L1 = `verify_board_state.py`).

### 10. Profile B red-CI refusal

- **Source**: `docs/concepts/release-management.md` §5.4 (~L451)
- **Passage**: "the AI must refuse to merge a PR whose CI is red".
- **Why this should be a rule**: existing universal invariant; phrased declaratively, but the binding-clause meaning is unambiguous.
- **Suggested target**: existing `docs/rules/no-merge-on-red.rule.md` (or, if not yet created in 5.E, new rule).

### 11. Parallel-wave anti-collision contract

- **Source**: `docs/concepts/release-management.md` §6.6 (~L510, L512)
- **Passage**: "Wave N slices that run in parallel must declare in `docs/openspec-slice.md` 'Anti-collision contract' the shared files they touch" + "When a parallel-wave slice must touch a shared file, it coordinates via …".
- **Why this should be a rule**: contract enforced socially today; L1 grep on `docs/openspec-slice.md` would surface drift.
- **Suggested target**: `docs/rules/parallel-wave-anti-collision.rule.md`.

### 12. Migration filename verbose form

- **Source**: `docs/concepts/release-management.md` §6.8 (~L548)
- **Passage**: "[Alembic migrations] must use the verbose form `<NNNN>_<topic>` (matching the filename)".
- **Why this should be a rule**: file-naming invariant; L1 pre-commit check on `alembic/versions/*.py` is straightforward.
- **Suggested target**: `docs/rules/alembic-migration-naming.rule.md`.

### 13. Slice openspec preflight

- **Source**: `docs/concepts/release-management.md` §7 (~L586)
- **Passage**: "Before the AI begins implementing a slice (before the first task commit on `slice/<change-id>`), it must [validate openspec preflight]".
- **Why this should be a rule**: phase-gate invariant; companion to existing apply-skill-enforcement rule.
- **Suggested target**: rolled into existing `docs/rules/apply-skill-enforcement.rule.md` or new `docs/rules/slice-preflight.rule.md`.

### 14. Subagent return envelope

- **Source**: `docs/concepts/agent-contract.md` §1 + §3 (~L7, L77)
- **Passage**: "parent agent must send when spawning, and the return envelope the child must emit" + "The child must return a single JSON document shaped as below".
- **Why this should be a rule**: machine-readable contract — the whole point of D17 (skills ⊥ rules) is that contracts like this are enforceable.
- **Suggested target**: `docs/rules/subagent-envelope-schema.rule.md` (paired L1 = jsonschema validate at spawn time, when the Task-tool harness ships).

### 15. Notification level declared

- **Source**: `docs/concepts/notification-policy.md` §1 (~L11)
- **Passage**: "Any notification emitted by a playbook-driven actor must declare one [of four levels]."
- **Why this should be a rule**: L1 check on `notify.py` callers; deterministic.
- **Suggested target**: `docs/rules/notification-level-declared.rule.md`.

### 16. Trace ID correlation

- **Source**: `docs/concepts/notification-policy.md` §1 (~L31)
- **Passage**: "Every warn and error notification must carry the emitting OTel trace_id so the recipient can jump directly to the trace."
- **Why this should be a rule**: enforceable jsonschema check on the notification payload.
- **Suggested target**: rolled into `docs/rules/notification-level-declared.rule.md` (payload-shape branch).

### 17. No secrets in notifications

- **Source**: `docs/concepts/notification-policy.md` §1 (~L46)
- **Passage**: "A notification payload must not include any value that secrets_scan.py would flag."
- **Why this should be a rule**: L1 check is already available (`scripts/secrets_scan.py`); the rule is the binding clause around it.
- **Suggested target**: `docs/rules/notification-no-secrets.rule.md` (paired L1 = secrets_scan.py invocation on payload).

### 18. Channel adapter contract

- **Source**: `docs/concepts/notification-policy.md` §1 (~L68)
- **Passage**: "A channel adapter lives under `scripts/notifications/<name>.py` and must [implement N requirements]" + "Fail open — a broken channel must not block the emit."
- **Why this should be a rule**: adapter API contract; trivial L1 check that every adapter exports the required functions.
- **Suggested target**: `docs/rules/notification-channel-adapter.rule.md`.

### 19. Parallel-review dismissal rationale

- **Source**: `docs/concepts/parallel-review.md` §X (~L185, L239)
- **Passage**: "the parent agent must emit a [dismissal rationale]" + "[verdict] must instead escalate per verdict-contract.md §3".
- **Why this should be a rule**: verdict-contract is already an always-loaded rule; this passage extends it for parallel-review.
- **Suggested target**: rolled into existing `docs/rules/verdict-contract.rule.md` (parallel-review branch).

### 20. Agentic-failures detector binding

- **Source**: `docs/concepts/agentic-failures.md` (~3 occurrences)
- **Passage**: passages declaring how detectors must emit OTel attributes and how the catalog must be appended via RFC.
- **Why this should be a rule**: catalog-mutation invariant; L1 check that every new failure-mode entry carries the OTel attribute key.
- **Suggested target**: `docs/rules/agentic-failure-catalog-schema.rule.md`.

## Length-cap overruns (not rule candidates — split queue for Sub-slice 5.F)

Five concept docs exceed the 300-line D7 cap and need a split during the Sub-slice 5.F harmonisation pass. Listed here so 5.F has the queue:

| Doc | Lines | Suggested split |
|---|---|---|
| `release-management.md` | ~877 | Sections 4.5 (AI-reviewer loop) and 6 (boards / openspec / migrations) are each ~300 lines and rule-dense — extract to rules per items 1–13 above; the residual concept doc shrinks under 300. |
| `ux-track.md` | ~582 | Likely needs split by UX-track phase. 5.F triage. |
| `event-and-data-patterns.md` | ~482 | Split by pattern category (events vs schemas vs migrations). |
| `agent-contract.md` | ~396 | Extract subagent-envelope schema to a rule (item 14); the residual concept doc is the explanation around it. |
| `runbook-bmad-openspec.md` | ~368 | This is misclassified — it belongs under `docs/runbooks/`. Slice 5.C should move it. |
| `fusion-integration-pattern.md` | ~336 | Slice 5.F triage. |
| `skills-distribution.md` | ~327 | Slice 5.F triage. |
| `notification-queue.md` | ~327 | Slice 5.F triage; some passages may extract to rules. |
| `dependency-injection-patterns.md` | ~301 | One line over; either trim intro or split by language (Python vs TS). |

## Misclassification candidates

- `runbook-bmad-openspec.md` — the filename literally starts with "runbook-". Move to `docs/runbooks/` in Slice 5.C.
- `v080-roadmap.md`, `v090-roadmap.md` (renamed in this slice from `v0.8.0-roadmap.md` / `v0.9.0-roadmap.md` because the slug regex `^[a-z][a-z0-9-]{1,40}$` forbids `.`) — these are historical roadmap snapshots, not reference material. Slice 5.F triage: either archive under `docs/archive/` or drop entirely. They will eventually go stale; leaving them in `docs/concepts/` muddles the reference corpus.
- `INDEX.md` — auto-generated by `gen_indexes.py`; references many docs no longer present (apply-fix-contract.md, bootstrap-directive.md, break-glass.md, conflict-resolution-policy.md, cross-slice-additive-extension.md). Slice 5.F regenerates it from the now-validated frontmatter.
