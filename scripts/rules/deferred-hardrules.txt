# Deferred paired_hardrule .py scripts.
# Each slug listed here has a rule doc with a non-null `paired_hardrule:` that
# names a script not yet on disk. The strict-mode validator downgrades the
# "not found" check to a no-op for these slugs while the implementations ship
# in a later slice.
#
# Audit-trail mirror: openspec/changes/slice-5f-harmonization/deferred-strict-failures.md
#
# When a paired hardrule ships, remove the matching slug from this file.

# Always-loaded rules (D16) — Slice 6 (v0.18.2) shipped all six paired hardrules.

# Workflow / contract rules — Slice 6 (v0.18.2) shipped all eight paired hardrules.

# Migrations / data-shape rules — implementation deferred to Slice 7 (polish).
alembic-migration-naming
cross-slice-additive-extension
migration-slot-reservation
agentic-failure-catalog-schema

# Notification trio — implementation deferred to Slice 7.
notification-channel-adapter
notification-level-declared
notification-no-secrets

# Apply / break-glass — implementation deferred to Slice 7.
apply-fix-contract
break-glass
hitl-approval-pattern
