---
description: Project dispatcher — read AGENTS.md first.
alwaysApply: true
---

# Cursor rules (example router — PROJECT level)

> Copy to `<project>/.cursor/rules/00-dispatcher.mdc`. Thin router.

Cursor: read [AGENTS.md](../AGENTS.md) first. That file is the project dispatcher.

Follow its bootstrap directive (section 0) before any task.

For universal norms, `AGENTS.md` inherits from `.ai-playbook/specs/*` (git submodule).
