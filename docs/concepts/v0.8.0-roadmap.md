# v0.8.0-roadmap.md

> **Status**: in flight. v0.8.0-rc1 (2026-04-30) shipped item 11 (release management contract). Items 1-10 still tracked for v0.8.0 stable. Each item carries enough context that whoever picks it up can act without re-discovering the rationale.

## Why deferred

v0.7.0 was scoped as "alignment + bridges + audit incorporation": resolve the BMAD↔OpenSpec drift, lift two patterns from the taste-skill / superpowers audits as new specs, add a soft-warn lint for SKILL.md description quality, and ship the slicing artefact contract. Master's directive was *"ship v0.7.0 stable + reversible BEFORE v0.8.0"* so that we can rollback if anything goes wrong. Items below were identified but held back from v0.7.0 to reduce blast radius.

## Design objectives (substantial — each warrants its own ADR + design conversation)

### 1. KISS single-versioning source

**Motivation** (Master, 2026-04-28): *"yo creo que solo deberia versionarse el ai-playbook y no las cosas que tiene dentro, KISS"*.

Today the playbook ships two semver streams (ai-playbook + consumer-d-skills) and consumers pin each independently in `consumers.yaml` `skills_pins:` + `AGENTS.md` `skills_sources:`. Drift surfaces between AGENTS.md and the actual submodule, and across consumers.

**Proposed**:
- Collapse to one canonical semver stream — `ai-playbook`.
- Skills live at a pinned commit (or sub-tag) inside the playbook tree (or as submodules of the playbook itself, not of each consumer).
- Consumers pin only `ai-playbook@vX`. The skills pin is implicit — whatever the playbook tag references.
- One propagation workflow (`propagate-playbook-bump`) handles everything.

**Impact**:
- Reduces: AGENTS.md drift, cross-consumer pin inconsistency, two propagation routes.
- Trade-off: a consumer cannot independently pin `skills@v0.5` while staying on `playbook@v0.6`. We bet that case is rare (and if it arises, fork the playbook).

**Investigation tasks**:
- ADR proposal: collapse-to-single-versioning.
- Spec proposal: how do `consumers.yaml.skills_pins` migrate? Migration window?
- Open question: do source repos like `consumer-d-skills` keep their own semver for external use, while the playbook pins them at a known-good SHA internally?

### 2. Complete the bmad-create-ux-design v1→v2 migration

In v0.6.0 we rewrote `templates/new-project/.claude/skills/bmad-create-ux-design/workflow.md` to v2 (three-step order, palette decoupling, OKLCH-canonical, etc. — per `specs/ux-track.md` v2.0.0). The `steps/` files underneath were not migrated and still produce the v1 monolithic `ux-design-specification.md`.

**Why deferred from v0.7.0**: consumer-c's M2 UX is already done, so this only affects future projects. Substantial work (15+ step files) — enough to warrant its own focused PR.

**Action**: rewrite each step under `bmad-create-ux-design/steps/` to produce the v2 artefact ladder (`docs/ux/inspiration.md` → `palette-options.html` → `variants/` → `DESIGN.md` → `jN.md` → `components.md`). Validate end-to-end on a fresh module.

### 3. Vendor / lift `systematic-debugging` from superpowers

The audit recommended `systematic-debugging/SKILL.md` from `obra/superpowers` (MIT) as a worker-side replacement for the `bmad-quick-dev` debug path during `/opsx:apply` Phase 3.2. Formalises a 4-phase root-cause sequence + the "3 failed fixes ⇒ question architecture" stop rule.

**Why deferred from v0.7.0**: touches the worker→QA contract; needs an ADR + a real test on Module 2 before generalising.

**Action**:
- Lift (don't vendor) the pattern into a new skill at `templates/new-project/.claude/skills/systematic-debugging/`.
- Cite source per `specs/output-completeness.md` provenance posture (no whole-file vendoring; cite repo + license at the top).
- Wire the "3 failed fixes" rule to emit `⛔ ARCHITECTURE QUESTIONED` per `verdict-contract.md` v1.1.0.

### 4. Two-stage review split (spec-compliance vs code-quality)

From the superpowers audit. Currently `bmad-code-review` conflates "did you implement the spec" with "is the code good". Splitting into two reviewers (sequential or parallel) lets compliant-but-ugly code get caught separately from incompliant code.

**Why deferred**: requires changes to `parallel-review.md` and the QA worker contract; not blocking.

**Action**: ADR on the split + revise `parallel-review.md` + revise `bmad-code-review` step structure.

### 5. Scan→Diagnose→Fix audit shape

From the taste-skill audit (`redesign-skill`). Three-phase audit format that reads tighter than free-form review prompts.

**Action**: adopt as the pattern for `bmad-code-review`, `bmad-review-adversarial-general`, and `bmad-review-edge-case-hunter`. Update each skill's `steps/` to use the format.

### 6. 3-dial parameterization for non-UX skills

From the taste-skill audit. The pattern (`DESIGN_VARIANCE` / `MOTION_INTENSITY` / `VISUAL_DENSITY`) generalises: a skill exposes 1–3 dials that conditionally tune its rules. Non-UX analogues: `RIGOR` / `VERBOSITY` / `RISK_TOLERANCE` for skills like `bmad-quick-dev` or `openspec-propose`.

**Action**: prototype on one skill (probably `bmad-quick-dev`), measure value, then generalise.

## Operational items deferred

### 7. Apply CSO description rewrites to existing skills

v0.7.0 ships the `check_skill_descriptions.py` lint as soft-warn + an audit doc with side-by-side proposed rewrites for every skill description that summarises the workflow. v0.8.0 reviews + applies the proposed rewrites in a focused PR.

**Action**: Master reviews the audit, approves the batch, applies to `templates/new-project/.claude/skills/<each>/SKILL.md`.

### 8. Migrate existing SKILL.md to the v0.7.0 schema (`## Anti-patterns` block)

v0.7.0 specifies the new SKILL.md schema (with required `## Anti-patterns` section) as canonical; existing skills predate it. Migrate opportunistically: when a skill is touched for any reason, add the section.

**Action**: target ~20 skills per release until all 69+ have the section. Track progress in this doc.

### 9. CI hardening of `output-completeness` and `verification-before-completion`

Both specs ship in v0.7.0 as documents. v0.8.0 ships `scripts/check_output_completeness.py` and `scripts/check_verification.py` (initial: warning; hardening: CI-blocking later).

**Action**: implement both linters; add to pre-commit; add to CI.

### 10. `openspec-sync-specs` skill

The `openspec-archive-change` skill referenced this skill but the skill doesn't exist (audit finding F4). v0.7.0 removed the broken reference. v0.8.0 decides: implement the skill (if there's value in a spec-sync step at archive time) or formally retire the reference.

**Action**: investigate intent (Jesse / OpenSpec upstream); decide; document.

### 11. ✅ Release management contract (shipped in v0.8.0-rc1, 2026-04-30)

**Motivation** (Master, 2026-04-29 in consumer-e Gate E discussion): "me gustaria que esta forma de trabajar fuera el default para todos los proyectos de ai-playbook, como podemos hacer?"

The hybrid flow had an undocumented seam: `runbook-bmad-openspec.md` §3 said "implementation in `slice/<id>` branch" but never normatively answered whether each `tasks.md` checkbox is its own branch + PR, what the canonical Status field schema should be, when CI gating triggers `In Progress → Review`, or how the dependency graph in `docs/openspec-slice.md` translates to merge order. That gap surfaced concretely when consumer-e reached Gate E and a contributor asked.

**Shipped in v0.8.0-rc1**:
- `specs/release-management.md` v1.0.0 (8 sections: branch model, PR shape, CI gates, project board schema, dependency-driven merge order, bootstrap automation, migration, anti-patterns).
- `scripts/bootstrap_gh_project.py` (one-command setup of any consumer's GH Project board: idempotent option addition, optional custom fields, optional draft items from `docs/openspec-slice.md`).
- `runbook-bmad-openspec.md` v1.1.0 §3.6 (one-paragraph summary pointing at the new spec).
- `runbook-bmad-openspec.md` §5 Gate F row updated (CI green prerequisite).

**Pending for v0.8.0 stable**:
- Validate the contract end-to-end via consumer-e Wave 0 (slices 1-3) → retro → if green, promote `v0.8.0-rc1 → v0.8.0`.
- Optional `.github/workflows/project-status.yml` template (auto Status transitions on CI pass + merge events). Doc placeholder in `release-management.md` §6.3.
- Optional `.github/workflows/dep-check.yml` template (hard enforcement of dependency-merged-before-merge). Doc placeholder in §6.2.
- Update `templates/new-project/AGENTS.md.tmpl` to reference `release-management.md` (deferred to v0.8.0 stable; rc1 keeps template change minimal).

## Process

When v0.8.0 opens, this file becomes the working backlog. Each item that ships gets struck through here and an entry in `CHANGELOG.md` v0.8.0. Items not shipped roll over to `v0.9.0-roadmap.md` (or are deleted with rationale).

## Cross-references

- [bmad-openspec-bridge.md](bmad-openspec-bridge.md) — v0.7.0 deliverable that this roadmap builds on.
- [output-completeness.md](output-completeness.md) — v0.7.0 spec; v0.8.0 ships the linter.
- [verification-before-completion.md](verification-before-completion.md) — v0.7.0 spec; v0.8.0 ships the linter.
- [verdict-contract.md](verdict-contract.md) v1.1.0 — `⛔ ARCHITECTURE QUESTIONED` literal that item 3 emits.
- [skills-distribution.md](skills-distribution.md) — schema where item 1 (single-versioning) lands.
