# contributing.md

> **Status**: stub, v0.1.0. Populated in **T14** (EX package) + **T22** (governance).

## For v0.1.0

- Solo maintainer (Arturo). No PRs yet.
- Breaking changes require an RFC under `rfcs/` (see `rfcs/README.md`).
- All commits: Conventional Commits.

## Planned sections (populated T14 + T22)

- RFC template + review process.
- Role matrix (author / reviewer / approver) — separate from k8s ServiceAccount RBAC.
- SLOs for playbook maintenance (deprecation watcher, drift detector, retro cadence).
- Channels: where to file an issue vs an RFC vs `FEEDBACK.md`.
- Rollout strategy for breaking changes (deprecation window + CHANGELOG callouts + migration guide update).
