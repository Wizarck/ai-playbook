---
template: ai-playbook/retro/monthly/v1
month: {{YYYY_MM}}
first_monday: {{YYYY_MM_DD}}
participant: {{HANDLE}}
---

# Monthly retro — {{YYYY_MM}}

> Created in **T14h** (template). Lives at `reports/retros/{{YYYY_MM}}/monthly.md`. Runs the first Monday of the month per `specs/retrospective-cadence.md` §1. The skeleton is auto-generated by `scripts/telemetry/report.py` (absorbed in Slice 6 v0.18.2); the human fills in narrative.

## 1. Lifecycle-check output (paste verbatim)

Run: `python -m scripts.telemetry.report monthly`. Paste the full report here — do not summarise yet; the raw output is evidence.

```
{{LIFECYCLE_CHECK_OUTPUT_VERBATIM}}
```

Expected sections (per `specs/retrospective-cadence.md` §4):

- Stale OpenSpec changes (no activity >30 days).
- Outdated memories (hindsight entries older than the relevant code).
- Drift findings (playbook ↔ consumer `AGENTS.md`).
- Override counters per gate per project.
- Notification volume per actor per level (per `specs/notification-policy.md` §5).
- Secrets-scan stats.

## 2. Systemic flags acted on

For every systemic flag in §1, either act or explain why not. Anti-pattern: flag shown, no response.

| Flag | Signal | Action taken or explicit deferral | Owner |
|---|---|---|---|
| {{FLAG}} | {{evidence}} | {{action / deferral reason + target month}} | {{handle}} |
| … | … | … | … |

A flag that is deferred for 3 consecutive months escalates to RFC.

## 3. Deprecation / migration progress

Source: `specs/migration-guide.md` (deprecation table) + consumer drift scan.

- **Deprecated features still in use**: {{LIST or None.}}
- **Features reaching removal this cycle**: {{LIST or None.}}
- **Migration recipes updated this month**: {{LIST or None.}}

If any deprecated feature's removal date falls within the next retro window, add a blocking action item.

## 4. Cost report summary

Source: `python -m scripts.telemetry.report monthly --json` output for the month (Slice 6 v0.18.2 absorbed the former `scripts/cost_report.py`).

- **Total cost**: {{$X.XX}}.
- **Top 3 task classes by spend**: {{CLASS}} ({{%}}), {{CLASS}} ({{%}}), {{CLASS}} ({{%}}).
- **Degradation-forced-ship incidents** (per `specs/break-glass.md` interaction with `degradation-modes.md`): {{N}}.
- **Month-over-month delta**: {{±X%}}; explanation if >20%: {{ONE_SENTENCE}}.

## 5. Retros archived this cycle

| Cadence | Count | Notable |
|---|---|---|
| Post-archive | {{N}} | {{link to worst-verdict retro}} |
| Weekly | 4 | {{link to most actionable}} |

If count is below expected (e.g. <4 weekly retros for a 4-week month), flag the skip as a systemic miss.

## 6. Action items

| # | Item | Owner | Due | Tracking |
|---|---|---|---|---|
| 1 | {{ACTION}} | {{handle}} | {{YYYY-MM-DD}} | {{GH#N or RFC link}} |
| … | … | … | … | … |

Cross-reference §2 flags — every systemic flag acted on should have a corresponding action item row.

## 7. Verdict

Per `specs/verdict-contract.md` §1. A monthly retro with no narrative on any systemic flag is the `retros-without-action-items` anti-pattern.

{{VERDICT_LITERAL_EXACTLY_AS_PER_CONTRACT}}
