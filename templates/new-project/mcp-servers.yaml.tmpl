# mcp-servers.yaml — PROJECT layer for {{PROJECT_NAME}}.
# Merged with ai-playbook/mcp-servers-base.yaml (universal) and personal layer
# via `python .ai-playbook/scripts/mcp/render.py --project {{PROJECT_NAME}}`.

schema: mcp-servers/v1
version: 0.1.0
layer: project
project: {{PROJECT_NAME}}

servers: {}
