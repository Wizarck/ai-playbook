# Generated by ai-playbook bootstrap. Append project-specific patterns above
# the playbook block; do NOT remove the playbook block markers below or the
# auto-regenerated `.claude/injected-context.md` will start being committed.

# >>> ai-playbook:begin id=playbook-patterns >>>
# ai-playbook integration
.ai-playbook/overrides.log
.ai-playbook/hindsight-queue.jsonl
.ai-playbook/notifications.jsonl
.ai-playbook/heartbeat-t18a.txt
.claude/injected-context.md

# per-consumer toggle + curation state
.ai-playbook/rules-toggle.json
.ai-playbook/rules-toggle-audit.jsonl
.ai-playbook/feature-flags.env
.ai-playbook/applied-config.json
.ai-playbook/applied-config.js
.ai-playbook/dashboard-data.js
.ai-playbook-state/

# rendered MCP configs — LOCAL build artifacts (base+project+personal merge).
# Personal/tenant servers must never land in a committed file, so the rendered
# output is per-machine: regenerate with `python .ai-playbook/scripts/mcp/render.py`.
# The COMMITTED source of truth is mcp-servers.project.yaml (no personal) +
# ~/.config/mcp-servers.yaml (personal, local-only, never committed).
.mcp.json
.gemini/settings.json

# apply_config backup files (default location is next-to-original; the
# central layout under .ai-playbook-state/backups/ is covered above)
*.bak
*.bak.????-??-??T??-??-??Z
# <<< ai-playbook:end playbook-patterns <<<
